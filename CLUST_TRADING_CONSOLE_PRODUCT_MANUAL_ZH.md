# Clustr Trading Console 产品手册

**文档性质：** 产品能力与安全边界说明  
**产品名称：** Clustr Trading Console  
**适用环境：** DeepSeek Harness Web，本地运行

## 1. 产品定位

Clustr Trading Console 是面向 AI Trader 的本地交易操作系统。它把多交易所行情、结构化判断、交易论点、确定性风控、权限控制、决策记录与复盘工具接入 DeepSeek Harness，使用户可以在同一会话内完成“观察—判断—反证—风控—审批—跟踪—复盘”。

产品遵循四项基础原则：

1. **先保全资本，再讨论收益。** 无法确定数据、规模、权限或订单状态时，新增风险的操作必须拒绝。
2. **事实、判断、规则、状态分离。** 交易所数据是事实，AI 输出是判断，风控代码是规则，订单回执是状态；四者不得互相冒充。
3. **公共行情不等于账户连接，账户连接不等于执行就绪。** 控制台分别展示各层状态。
4. **用户保有最终控制权。** 默认只读，授权有范围和期限，新增风险需要确定性风控与用户审批。

Clustr Trading Console 不提供收益保证，不代替持牌投资顾问，不托管用户资产，也不会把模型置信度当作交易许可。

### 1.1 选择性启用

安装插件不会替换 DeepSeek Harness 的普通模式。新会话默认使用 Harness 内置预设；只有当用户在 Agent Preset 选择器中明确选择 **Clustr Trading Console** 时，当前会话才加载 Clustr 背景、交易入口、安全状态与 Console 上下文。

预设按会话隔离。同一用户可以在不同工作区中同时保留普通 Harness 会话与 Clustr 交易会话。已经产生消息的会话保持原有预设；如需切换模式，应新建会话后再选择预设，避免交易规则进入无关任务，或让普通任务继承交易工具上下文。

## 2. 九层统一操作内核

九层能力围绕一条交易工作流组织，而不是九个互相割裂的功能页面。

| 层级 | 名称 | 当前机制 | 明确边界 |
|---|---|---|---|
| 1 | 市场情报图谱 | 接入 OKX、Binance、Bybit、Hyperliquid 公共行情；统一 ticker、K 线与订单簿结构；计算跨所中位价、差异与数据新鲜度 | 不把不同来源无声混合；交易所不可达时明确降级 |
| 2 | 协作判断 | 记录宏观、结构、催化、量化与个人约束等角色意见，计算方向与分歧 | 这是结构化意见汇总，不是自动运行的 Agent 团队；意见不能覆盖风控规则 |
| 3 | 交易论点 | 记录标的、方向、入场条件、预期路径、失效条件、期限与风险预算；使用合法状态迁移 | 缺失失效条件、期限已过或风险预算无效时拒绝创建或推进 |
| 4 | 对照方案 | 为同一论点建立原始计划、半风险、延后入场与保持空仓方案 | 当前记录对照定义与观察，不宣称为实时计价的独立影子账户 |
| 5 | K 线回放 | 历史 K 线按步骤释放，推进前隐藏未来 K 线 | 当前是 K 线级回放，不等于完整逐笔事件、撮合、费用与滑点模拟器 |
| 6 | 风险内核 | 确定性检查数据时效、滑点、杠杆、名义价值、交易精度、单笔风险与当日亏损；成功后签发五分钟单次许可 | AI 判断不能修改硬限制；名义价值、合约乘数、最小数量或价格步进不明时拒绝新增风险 |
| 7 | 自主权 | 观察、建议、准备、逐笔审批、限域执行、仓位守护六级权限；授权包含交易所、标的、次数、风险上限与期限 | 当前安装为只读，执行权限关闭；过期、越域或次数耗尽立即拒绝 |
| 8 | 交易记忆 | 保存交易结果、计划遵循度、错误标签与复盘结论，支持重复错误汇总 | 质量分数属于调用者或人工标签，不代表系统已经完成统计学习 |
| 9 | 决策溯源 | 记录事实来源、判断方法、规则裁决、状态迁移、审批与订单标识；敏感字段脱敏 | 本地记录帮助复核，不把缺失的交易所回执补写成确定事实 |

九层统一操作内核已经建立，但每层只按上表所述机制承担责任。产品不会因为一个容器存在，就把它描述成已经闭环运行的自动系统。

## 3. 标准使用流程

### 3.1 观察

在 Clustr Console 查看关注标的、K 线、成交量、均线与已连接账户。绿色 K 线表示上涨，红色表示下跌；MA7 使用黄色，MA25 使用蓝色。

Console 中的“交易所账户”表示用户可以连接和管理的账户，不代表图表默认从这些交易所混合取数。账户连接、账户读取与交易执行分别显示：

- **账户连接：** 本机保险库中是否存在该账户的凭证或公开地址；
- **账户读取：** 私有接口是否成功返回余额与持仓；
- **交易执行：** Clustr 是否已经建立受控执行通道，并且当前权限、风控和审批是否允许。

市场结构区提供独立的“行情场所、市场类型、交易品种”选择器。用户可以搜索交易所公开目录中的交易对，例如 BTC、SOL、DOGE、现货或永续合约。搜索结果选中后，K 线按该交易所和市场类型单独加载，不会无声混用其他来源。

跨所判断可以请求：

- “比较 BTC-USDT 在 OKX、Binance、Bybit 与 Hyperliquid 的价格一致性。”
- “查看 BTC-USDT 的订单簿、新鲜度和不可用数据源。”

### 3.2 启用分析

盘面方法默认不运行。用户需要打开“市场分析”，选择方法，再点击“运行分析”。切换标的或周期会清除旧结果，旧请求不得回写到新图表。

工具库包含威科夫、艾略特波浪候选、ATR 标准化江恩角度、道氏理论、一目均衡表、斐波那契回撤、成交量分布、SMC 市场结构、海龟/Donchian 突破、布林带 + RSI + MACD、SuperTrend + ADX/ATR，以及 VWAP + OBV + MFI。各方法的公式依据、输入要求与局限记录在 `ANALYSIS_METHODS.md`。

所有结果使用“结构匹配度”，它是启发式证据分数，不是经过校准的成功概率。结果必须同时给出：

- 支持证据；
- 反证或证据不足；
- 候选事件；
- 失效条件；
- 无法判断状态。

### 3.3 建立交易论点

示例指令：

> 为 BTC-USDT 建立一个观察中的做多论点：只有放量站稳关键阻力才触发；跌回结构下沿失效；有效期 24 小时；风险预算 0.5%。

论点状态按以下路径推进：观察 → 触发 → 验证 → 批准 → 执行 → 关闭。失效或过期可以终止路径，不能绕过验证直接进入执行。

### 3.4 反证与对照

协作判断层用于记录不同角色的立场和证据，对照方案用于比较“原计划、半风险、延后、空仓”。这两层服务于反证，不承担最终交易许可。

### 3.5 风险与审批

新增或扩大风险前，必须先由风险内核评估，并生成与标的、名义价值绑定的单次许可。真实写操作还必须同时满足：

- 当前配置允许写操作；
- 自主权等级和授权范围允许；
- 市场与账户数据仍然新鲜；
- 订单规模、滑点、杠杆、止损与亏损阈值通过；
- DeepSeek Harness 审批栈返回本次允许；
- 单次风险许可尚未过期或使用。
- 紧急停止没有启用。

任何一项不满足，订单都不会发送。自主权次数与风险许可在发送前原子消费，同一个许可不能被并发订单重复使用。

### 3.6 跟踪、Session Tape 与复盘

每次交易工具收到下单、撤单、改单或平仓指令后，Session Tape 会按当前会话记录标准化指令、风控、授权、审批、发送、交易所回执与核对阶段。Console 显示本会话指令数量、响应时间 P50/P95，以及只基于交易所可核验成交均价计算的实际滑点。没有成交均价时滑点保持为空，不能用委托价或推测值代替。

Session Tape 不保存 API Key、Secret、Passphrase、风险许可或完整交易所原始响应。响应时间包含风控与人工审批等待，因此用于解释完整用户路径，不冒充纯网络延迟。订单、审批与本地状态变化同时进入决策记录。交易结束后，用户可以记录结果、是否遵守计划、错误标签和复盘结论，再查询重复出现的执行偏差。

### 3.7 响应路径与工具边界

Clustr 会话只向模型展示十个任务级工具：上下文、市场数据包、分析、风险、订单、论点、模拟、记忆、溯源和运行状态。交易所原始方法以及通用开发工具不直接进入模型工具列表，而是在任务级工具内部按固定映射调用。这一边界减少模型选择错误，也避免模型把任意方法名当作可执行订单入口。

系统依据用户意图分配响应路径：

- 账户、余额、持仓、报价、连接和状态查询使用最低推理预算；完成必要读取后可以直接返回；
- 盘面分析、交易计划和明确订单操作使用较高推理预算；
- 用户明确要求深度推演、对抗审查或压力测试时使用最大推理预算；
- 市场分析读取压缩后的市场数据包，包含来源时间、新鲜度、趋势、波动、支撑阻力、价差与深度证据，不把整段原始 K 线塞入会话；
- 普通心跳更新时间不会改变系统提示。只有执行配置、自主权、紧急停止、账户状态或未知订单等有意义的变化才推进状态版本。

推理预算只影响模型思考深度与响应成本，不影响权限。订单仍必须经过确定性风险内核、限域授权、一次性审批、执行账户核对和订单状态机；模型不能通过措辞绕过这些闸门。

## 4. 交易所能力矩阵

公共行情、私有账户和交易执行是三个独立状态。

| 交易所 | 账户连接方式 | 账户读取 | 交易执行能力 | Clustr 当前边界 |
|---|---|---|---|---|
| OKX | API Key、Secret、Passphrase；账户名称可区分多个连接 | 余额与持仓分账户读取；支持模拟与真实环境隔离 | OKX Agent Trade Kit 已接入 Clustr 风控、授权与 Harness 审批链 | 安全启动时为只读；符合资格的用户可显式开启限时执行窗口，新增风险只有在所有确定性闸门通过后才可能发送 |
| Binance | API Key 与 Secret；支持多个账户名称 | 读取现货余额、USDⓈ-M 账户、持仓与挂单；产品或地区权限不足时按范围显示部分可读 | Clustr 已接入官方 Spot 与 USDⓈ-M 签名 REST：下单、订单查询、撤单及 U 本位风险降低平仓 | 默认只读；用户只能为一个具名账户开启限时执行，交易必须通过交易所过滤器、时钟同步、新鲜行情、风险许可、授权范围与逐笔审批 |
| Bybit | API Key 与 Secret；支持多个 Live/Testnet 账户名称 | 通过 V5 私有 API 读取 Unified Wallet、挂单，以及 USDT、USDC 线性和反向合约持仓 | Clustr 通过固定版本的 Bybit Official Trading MCP 执行现货、Linear 与 Inverse 的下单、查询、撤单和风险降低平仓 | 默认只读；Linear 下单先经过官方预检查，所有 ACK 继续通过实时订单与历史订单核对；地区不可达时明确显示连接异常，不切换未知代理 |
| Hyperliquid | 未开放 | 未开放 | 未开放 | 公共市场行情仍可使用；Console 不提供账户连接、账户读取或交易执行入口，并拒绝任何私钥与助记词 |

公共行情是图表基础设施，不是“交易所账户”列表的状态含义。“账户已连接”只表示本机存在连接材料；“账户可读”表示私有接口成功；“执行已启用”还要求受控连接、配置、权限、风险和审批全部满足。

官方接入参考：

- Bybit Trading MCP：`https://github.com/bybit-exchange/trading-mcp`
- Binance Spot REST 与安全规则：`https://developers.binance.com/en/docs/products/spot/rest-api`
- Binance Spot 交易接口：`https://developers.binance.com/en/docs/catalog/core-trading-spot-trading/api/rest-api/trade`
- Binance USDⓈ-M 交易接口：`https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/trade`
- Hyperliquid Info / Exchange API：`https://hyperliquid.gitbook.io/hyperliquid-docs/for-developers/api`
- Hyperliquid 官方 Python SDK：`https://github.com/hyperliquid-dex/hyperliquid-python-sdk`

## 5. 订单状态语义

| 状态 | 含义 |
|---|---|
| eligible | 输入满足基础规则，可以继续评估 |
| approved | 风控、授权与用户审批已经通过 |
| accepted | 交易所明确接受订单 |
| partially filled | 部分成交，剩余数量仍需跟踪 |
| filled | 交易所确认全部成交 |
| rejected | 本地规则、审批或交易所拒绝 |
| reconciling | 请求超时或断线，结果未知，正在依据 clientOrderId 核对 |

`eligible` 不等于 `approved`，`approved` 不等于 `accepted`，`accepted` 也不等于 `filled`。发送超时后严禁盲目重试，以免产生重复订单。

日累计名义额度、风险预留与待核对订单写入本地原子账本。服务重启不会清零当天额度，也不会遗忘 `reconciling` 订单；重启后系统继续依据交易所客户端订单编号核对。

## 6. 凭证与密钥安全

### 6.1 静态存储

Clustr 使用同一个插件包，并根据操作系统自动选择凭证保险库：macOS 使用 Keychain 与 Security Framework，Windows 使用 Credential Manager，Linux 使用 Secret Service。秘密通过标准输入交给平台后端，不进入命令行参数、HTTP 错误、审计记录或产品日志。Hyperliquid 私钥不会被接收或保存。

保存前会先连接交易所，验证读取范围和可检测到的权限；验证不通过时不会写入系统保险库。保存、读取与删除只返回脱敏状态，删除失败不会伪装成删除成功。系统保险库缺失、锁定或不受支持时，账户连接保持关闭，不回退到 `.env`、明文 JSON 或浏览器存储。

OKX 官方 Agent Trade Kit 在运行时通过子进程环境接收所选账户凭证。这些值不会写入用户的 Shell 环境、插件配置、日志、浏览器或决策记录，但同一登录用户下已经被攻陷的恶意进程仍可能检查进程内存或运行环境。操作系统保险库不能消除这类本机失陷风险。

平台要求：Windows 10/11 使用系统 PowerShell 与 Credential Manager；Linux 需要一个已解锁的 Secret Service 提供方以及 `secret-tool`。当前平台与保险库状态会显示在设置页，用户不需要选择 macOS 版或 Windows 版插件。

### 6.2 推荐权限

- 优先使用只读 API Key；
- 需要交易时使用独立子账户和最小权限；
- 禁止 Withdraw、Transfer 与地址白名单管理权限；
- 在交易所侧设置 IP 白名单；
- 模拟环境与真实环境使用不同凭证；
- Hyperliquid 账户连接与交易执行未开放；控制台不接收 API Wallet 私钥、主钱包私钥或助记词。

### 6.3 威胁模型

操作系统保险库保护静态存储和普通文件泄漏，但无法抵御已经控制当前登录会话的恶意程序，也无法抵御同权限进程在凭证被合法使用时窃取内存。用户仍需维护操作系统安全、最小权限、子账户、额度限制与交易所侧撤销能力。

Clustr 永远不需要助记词。任何要求输入助记词或主钱包私钥的界面、脚本或提示都应被视为异常。

## 7. 本地数据与隐私

| 数据 | 位置 | 保护方式 |
|---|---|---|
| 交易所凭证 | macOS Keychain、Windows Credential Manager 或 Linux Secret Service | 操作系统保险库访问控制；秘密不写入普通文件 |
| 凭证账户索引 | `~/.dsh/clustr/credential-index.json` | 只保存交易所与账户名称，不保存任何凭证；原子写入 |
| 九层操作状态 | `~/.dsh/clustr/operating-core.json` | 本地文件，权限 `0600`，原子写入 |
| 决策记录 | `~/.dsh/clustr/decision-tape.json` | 本地文件，权限 `0600`，最多保留 500 条，敏感字段脱敏 |
| 交易指令 Session Tape | `~/.dsh/clustr/session-tape.json` | 按会话保存标准化交易阶段，权限 `0600`，最多保留 300 条，字段白名单与敏感值过滤 |
| 行情与账户缓存 | 当前进程内存 | 服务重启后消失；过期数据不得用于新增风险 |

操作状态与决策记录没有额外加密，依赖本机文件权限。文件损坏或读取权限异常会明确报告，不会静默恢复为空状态。开源部署者应根据自身威胁模型决定是否增加磁盘加密、备份与保留策略。

## 8. 故障与安全降级

- **交易所不可达：** 显示连接异常；该数据源不参加正常共识，不自动切换未知代理。
- **IP 白名单不匹配：** 显示交易所错误码与“当前网络出口未加入白名单”；用户应把实际代理出口加入交易所白名单，而不是本机局域网地址。
- **数据陈旧：** 显示时间或警告；新增风险评估拒绝通过。
- **账户未知：** 不把缓存当作当前余额或持仓；涉及账户的写操作拒绝。
- **名义价值未知：** 风险内核拒绝，不以 `null` 或估算缺失放行。
- **订单结果未知：** 状态进入 `reconciling`，使用稳定 clientOrderId 查询，禁止盲目重试。
- **系统保险库失败：** 返回固定、脱敏错误；凭证不出现在页面或日志，也不降级到明文文件。
- **本地状态损坏：** 报告恢复问题并保留安全边界；不会因为读取失败而提升权限。
- **自主权过期：** 自动降级；越过交易所、标的、次数或风险范围的请求被拒绝。
- **紧急停止：** 立即撤销活动许可并降回观察；恢复后仍需重新授权、重新风控和逐笔审批。

设置页的“查询当前网络出口 IP”只在用户点击时向 `api.ipify.org` 发起不含交易所凭证的请求，用于确认需要加入交易所白名单的公网出口。

## 9. 自主权等级

| 等级 | 能力 | 是否可以发送新增风险订单 |
|---|---|---|
| L0 观察 | 读取行情和状态 | 否 |
| L1 建议 | 输出分析、论点与反证 | 否 |
| L2 准备 | 准备订单参数与风险摘要 | 否 |
| L3 逐笔审批 | 每一笔都需要用户本次批准 | 通过全部闸门后可以 |
| L4 限域执行 | 在明确交易所、标的、次数、风险和期限内执行 | 通过全部闸门后可以 |
| L5 仓位守护 | 只在预先授权范围内执行风险降低动作 | 仅限明确授权的风险管理动作 |

只读配置优先级最高。即使自主权状态较高，只读配置仍会阻止写操作。用户可以随时撤销授权或启用紧急停止；授权重启后不得意外升级。

## 10. 开源使用边界

面向他人分发时，应同时提供：

- 锁定的依赖版本和可验证的更新来源；
- 安全问题报告渠道；
- 默认只读配置；
- 交易所能力矩阵与地区限制；
- 凭证最小权限指南；
- 本地数据导出、删除和备份说明；
- 明确的非托管、非投资建议和无收益保证声明。

使用者需要自行判断所在地区的交易所可用性、API 条款、税务与合规责任。Clustr Trading Console 不会通过未知第三方代理规避交易所的区域限制。

## 11. 当前安全结论

当前安装适合作为本地、默认只读的 AI Trader 工作空间：全市场品种搜索、多交易所 K 线、分账户余额与持仓、手动盘面分析、交易论点、风险许可、自主权边界、跨平台系统保险库与决策记录已经接入同一控制台。

当前受控执行路径面向 OKX、Binance 与 Bybit。系统以只读状态安全启动；只有符合配置资格的用户主动选择唯一的交易所与具名账户，开启限时、限标的、限次数的执行窗口，并通过风险、授权和逐笔审批后，才可能发送订单。Bybit 使用官方 Trading MCP，并把 Live 与 Testnet 隔离；Hyperliquid 只保留公共市场行情，账户连接、账户读取与交易执行均标记为未开放。系统遇到不确定数据、权限、行情环境或订单状态时，以拒绝和核对为默认行为；提交超时后使用稳定 clientOrderId/orderLinkId 查询，绝不盲目重下。
